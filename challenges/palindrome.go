package challenges

/*
func isPalindrome(x int) bool {
    y := strconv.Itoa(x)
    runes := []rune(y)
    var inverted []rune
    for i := len(runes) - 1; i >= 0; i-- {
        inverted = append(inverted, runes[i])
    }
    if reflect.DeepEqual(inverted[:], runes) == true {
        return true
    }
    return false
}

https://leetcode.com/problems/palindrome-number/solutions/7142995/palindrome-by-vinycxuz-mq8m/
*/
